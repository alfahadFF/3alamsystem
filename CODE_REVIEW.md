# 🔍 مراجعة تقنية شاملة — alfaprosys
### نظام إدارة ومحاسبة المطاعم · خارطة الطريق إلى مستوى عالمي

*تاريخ المراجعة: 2026-08-31 · بُنيت على فحص فعلي للملفات، لا على الانطباع*

---

> ## ⚠️ تنبيه: تصنيف هذه المراجعة صُحّح
>
> البنود **#1 (لا حفظ) · #2 (`submitOrder`) · #5 (لا مصادقة) · والأوفلاين** وردت أدناه بوصفها "اكتشافات حرجة"، وهي في الحقيقة **قرارات تطويرية مقصودة** في مرحلة تطوير الواجهة، وليست عيوبًا مجهولة.
>
> **المرجع الأحدث: `ROADMAP_v2.md`** — فيه التصنيف الصحيح، وخطة الربط عبر `data.js`، وحل مشكلة التكلفة، ومعمارية النسختين.
>
> ما يبقى قائمًا من هذه المراجعة: **علاقة الوصفة المعكوسة** (#3) · **XSS في 14 شاشة** (#5) · **تكرار الدوال** (#7) · **غياب `tenant_id`**.

---

## 1️⃣ ما هو موجود فعلاً (مُقيس بالأرقام)

| المكوّن | العدد |
|---|---|
| صفحات HTML | 16 صفحة + `F.html` (نموذج مستقل) |
| ملفات JS | 17 ملفاً · **11,877 سطراً** |
| ملفات CSS | 10 ملفات |
| الكيانات في `data.js` | 12 كياناً |
| الأصناف | **119** صنفًا |
| التصنيفات | 7 · الطاولات 12 · العملاء 5 · الموظفون 4 · المخزون 7 · العقود 2 |
| العملة | ل.س · الأعداد صحيحة (60000 لشاورما) ✅ قرار سليم |

**نقطة قوة حقيقية:** استخدام أعداد صحيحة للأموال بدل الكسور العشرية. هذا قرار صحيح مهنياً ويجب الحفاظ عليه.

**ملفات النصوص الثلاثة** (`suggestions.md`, `management_screens_plan.md`, `cashier_ideas.md`) ممتازة كمحتوى — فيها نضج في فهم عمل المطعم. المشكلة ليست في الأفكار، بل في الأساس التقني الذي ستُبنى عليه.

---

## 2️⃣ الاكتشافات الحرجة (تمنع الوصول لأي مستوى احترافي)

### 🔴 #1 — لا يوجد حفظ إطلاقًا. أي تحديث للصفحة يمحو كل شيء

```
نتيجة الفحص على الملفات الـ17:
  localStorage ............ 0 استخدام
  fetch / supabase / axios  0 استخدام
```

كل التعديلات تُكتب في الذاكرة فقط، مثل `DATA.contracts = contracts` في `contracts.js:379`، و`DATA.items.push(newItem)` في `menu_admin.js:593`. بمجرد F5 يعود كل شيء لنقطة الصفر.

### 🔴 #2 — شاشة البيع لا تُسجّل الفواتير أصلًا (الدالة شكلية)

السطر 847 من `pos.js`:

```js
function submitOrder(){ showToast(`تم إصدار الفاتورة #${invoiceSeq}`,'🖨️'); invoiceSeq++; cart=[]; renderPOS(); }
```

الدالة تعرض رسالة "تم إصدار الفاتورة" وتفرّغ السلة — لكنها **لا تضيف شيئًا إلى `DATA.invoices`**. تحققت: لا يوجد أي `push` إلى الفواتير في كامل `pos.js`.

**الأثر المتسلسل:** بما أن البيع لا يُسجَّل، فإن مبيعات اليوم والتقرير اليومي والـ Z-Report وخصم المخزون كلها تُحسب من بيانات تجريبية ثابتة لا من عمليات حقيقية. أي أن **النظام يعرض أرقامًا لا علاقة لها بما يبيعه المطعم فعلًا**. هذا أخطر ما في المراجعة.

### 🔴 #3 — علاقة الوصفة (Recipe) معكوسة → حساب التكلفة مستحيل

```
المادة الأولية في المخزون: "دجاج كامل للبروستد"  (تكلفة الوحدة 35000)
  └─ recipe[] تشير إلى:
       item_025 → بروستد وجبة دجاجة كاملة   (سعر بيع 145000)
       item_027 → بروستد طاولة دجاجة كاملة  (سعر بيع 160000)
       item_026 → بروستد وجبة نصف دجاجة     (سعر بيع  80000)
       item_028 → بروستد طاولة نصف دجاجة    (سعر بيع  90000)
       ...
```

المادة الخام تحمل وصفةً تشير إلى **أطباق جاهزة**. الصحيح عكس ذلك تمامًا: الوصفة ملكٌ للصنف في المنيو، وتشير إلى المواد الخام.

**يؤكده فحص التكلفة:**
```
cost_mode  = "manual"   في 119 من 119 صنفًا
cost_manual > 0         في   0 من 119 صنفًا
```
أي **لا توجد أي بيانات تكلفة**. لا تكلفة بضاعة مباعة (COGS)، لا هامش ربح، لا قائمة أرباح وخسائر. وفي نظام هدفه "محاسبة شاملة" هذا هو القلب.

### 🟠 #4 — ملاحظة في `suggestions.md` غير دقيقة

الملف يذكر: *"الحقل موجود في data.js لكن الربط مع pos.js لم يُبنَ بعد"*.
**الواقع:** `contract_price` موجود في **0 من 119** صنفًا. أسعار العقود موجودة داخل `contracts[].items[].price`. أي أن المطلوب ليس "ربطًا" بل **قرار في تصميم المخطط أولًا** (قوائم أسعار — انظر القسم 4).

### 🟠 #5 — ثغرة XSS مخزّنة في 14 من 16 شاشة

```
innerHTML .................. 101 استخدام
escapeHtml .................  51 استخدام (معرّفة في app.js فقط)
```

الشاشات التي تستخدم `innerHTML` **بصفر** `escapeHtml`:
`cash_reports` · `cashier_session` · `contracts` · `customers` · `edit_invoice` · `employees` · `expenditures` · `inventory` · `invoices` · `manager` · `menu_admin` · `open_invoices` · `reports` · `sales`

والأخطر: هذه الصفحات **لا تحمّل `app.js` إطلاقًا** (كل صفحة تحمّل `data.js` + سكربتها فقط)، فالدالة `escapeHtml` غير معرّفة فيها أصلًا. أسماء العملاء والموظفين والملاحظات تُحقن مباشرة في الصفحة — وبمجرد ربط قاعدة بيانات يصبح هذا استغلالًا فعليًا.

### 🟠 #6 — لا توجد مصادقة

```js
sessionStorage.setItem('alfaprosys_role', roleKey);   // app.js:34
```
أي مستخدم يغيّر هذا المفتاح من أدوات المطوّر فيصبح مديرًا. لا PIN ولا كلمة مرور ولا توكن (نتيجة البحث: صفر).

### 🟡 #7 — تكرار ضخم للكود وغياب أي طبقة مشتركة

| الدالة | عدد مرات تعريفها |
|---|---|
| `fmtNum` | **15** |
| `showToast` | 13 |
| `toggleNav` / `navLink` / `closeNav` | 10 لكل واحدة |
| `renderApp` | 8 |
| `fmt` | 4 |

كل صفحة جزيرة معزولة. أي تعديل على التنسيق أو التنقّل يجب تكراره 10–15 مرة.
كذلك: **لا `package.json`، لا Git، لا اختبارات، لا linter.**

---

## 3️⃣ جواب سؤالك المباشر: كيف تربطها بقواعد البيانات؟

### ⚠️ لا تربطها الآن بالطريقة البديهية

الفخ: أن توزّع نداءات `supabase.from('invoices').insert(...)` داخل 14 ملف JS. حينها ستعيد كتابة كل شاشة **مرتين** — مرة للربط، ومرة لإصلاح ما انكسر.

### ✅ الحل: طبقة وصول للبيانات (Data Access Layer)

ابنِ واجهة واحدة الآن، ونفّذها محليًا، ثم استبدل التنفيذ فقط:

```
assets/js/
├── db.js              ← الواجهة الوحيدة التي تعرفها الشاشات
├── store/
│   ├── local.js       ← التنفيذ الحالي على localStorage
│   └── supabase.js    ← التنفيذ المستقبلي (نفس الأسماء تمامًا)
└── config.js          ← يحدّد أي تنفيذ يُستخدم
```

```js
// db.js — الواجهة
const db = {
  items:    { list, get, create, update, remove },
  invoices: { list, create, update, void: voidInvoice },
  stock:    { move, balance },          // دفتر حركات، لا رصيد مباشر
  payments: { record, listByInvoice },
  audit:    { log },
};
```

الشاشات تنادي `await db.invoices.create(order)` ولا تعرف شيئًا عن التخزين.
**النتيجة:** ربط قاعدة البيانات يتحوّل من "إعادة كتابة النظام" إلى "كتابة ملف واحد".

هذه هي **أعلى خطوة عائداً على الجهد** في مشروعك كله.

---

## 4️⃣ المخطط المقترح لقاعدة البيانات

صُمّم لمعايير عالمية، لا لنسخة تجريبية:

```sql
-- الهوية والصلاحيات
branches            -- تعدد الفروع: شرط أساسي عالميًا
users, roles, permissions, role_permissions   -- RBAC حقيقي
audit_log           -- سجل غير قابل للتعديل: من/متى/ماذا/لماذا

-- المنيو والتسعير
categories, subcategories
items, item_modifiers           -- الإضافات (بدون حار، ثوم زيادة)
price_lists, price_list_items   -- ← هنا يُحلّ سعر العقود بشكل صحيح
                                --   بدل حقل contract_price داخل الصنف

-- التكلفة (إصلاح الاتجاه المعكوس)
recipes        (item_id)                     -- الوصفة ملك الطبق
recipe_lines   (recipe_id, inventory_item_id, qty)

-- المخزون بدفتر حركات
inventory_items
stock_movements  -- append-only: لا تُعدّل qty أبدًا، بل سجّل حركة

-- المشتريات والموردون
suppliers, purchase_orders, purchase_order_lines, goods_receipts

-- العملاء والعقود
customers
contracts, contract_items, contract_installments

-- البيع
invoices, invoice_lines
payments, payment_methods
shifts, cash_movements          -- الوردية ودرج النقد

-- المحاسبة
expenses, expense_categories
employees, attendance, payroll
settings
```

### ستة مبادئ محاسبية تفصل النظام العالمي عن النظام المحلي

1. **دفاتر غير قابلة للتعديل (append-only).** لا تكتب `qty = 10` أبدًا؛ بل أدخل حركة `+10` أو `-3`. ينطبق على المخزون والمدفوعات وتغييرات الأسعار.
2. **لا تعدّل السعر في مكانه.** استخدم `price_lists` مع `effective_from / effective_to`. وإلا تغيّرت فواتير الشهر الماضي عندما ترفع سعرًا اليوم.
3. **الأموال أعداد صحيحة** — أنت مطبّقها بالفعل ✅ حافظ عليها، ولا تقترب من `float`.
4. **مفتاح تفرد (idempotency key)** عند إنشاء الفاتورة. نقطة البيع دون إنترنت ستعيد المحاولة، وبدونه ستُسجَّل الفاتورة مرتين.
5. **حذف ناعم + سبب** للفواتير الملغاة. الإلغاء ليس حذفًا؛ هو قيد عكسي معلَّل.
6. **التكلفة بالمتوسط المرجّح** للمادة، محسوبة من أوامر الشراء ← تكلفة الوصفة ← هامش الطبق.

---

## 5️⃣ خارطة الطريق

### المرحلة 0 — أساسيات (هذا الأسبوع، ~3 أيام)
- [ ] `git init` + أول commit (بلا Git لا يوجد تطوير جاد)
- [ ] **إصلاح `submitOrder()`** لتُدرج الفاتورة فعليًا في البيانات
- [ ] حفظ محلي في `localStorage` حتى لا يضيع العمل عند التحديث
- [ ] توحيد `fmtNum`/`showToast`/التنقّل في `common.js` واحد
- [ ] حقن `escapeHtml` في كل الشاشات الـ14 (أمني، ليس تجميلًا)

### المرحلة 1 — طبقة البيانات والمخطط (~أسبوعان)
- [ ] `db.js` + `store/local.js`
- [ ] بناء المخطط أعلاه في Supabase مع Row Level Security
- [ ] `store/supabase.js` والتبديل بـ config

### المرحلة 2 — محرك التكلفة (~أسبوعان)
- [ ] عكس اتجاه الوصفات: `recipes` على الأصناف
- [ ] إدخال تكلفة الـ119 صنفًا (حاليًا كلها صفر)
- [ ] حساب COGS وهامش الربح لكل طبق وتصنيف

### المرحلة 3 — المحاسبة الفعلية (~3 أسابيع)
- [ ] قائمة أرباح وخسائر حقيقية (إيراد − COGS − مصاريف)
- [ ] دفتر الأستاذ العام المبسّط (Double-Entry Lite)
- [ ] الضرائب والرسوم حسب الدولة
- [ ] تقرير الذمم المستحقة (المذكور في `suggestions.md`)

### المرحلة 4 — تعدد المستخدمين والفروع
- [ ] مصادقة حقيقية + RBAC
- [ ] `audit_log` كامل
- [ ] توحيد عدة فروع

### المرحلة 5 — التشغيل المتقدم
- [ ] PWA يعمل دون إنترنت مع مزامنة وحل تعارضات
- [ ] شاشة المطبخ (KDS) + طابعات
- [ ] الطلبات الأونلاين وتكامل شركات التوصيل
- [ ] الولاء والبطاقات المسبقة ومحرك العروض

### المرحلة 6 — مستوى عالمي
- [ ] اختبارات آلية + CI/CD
- [ ] توثيق API + Webhooks
- [ ] نسخ احتياطي واستعادة بمؤشرات RPO/RTO معلنة
- [ ] تعدد اللغات والعملات

---

## 6️⃣ ما معنى "مستوى عالمي" فعليًا؟

مقارنة بـ Oracle Simphony و Toast و Lightspeed و Square و Foodics:

| الميزة | لديك؟ |
|---|---|
| حفظ البيانات | ❌ لا يوجد |
| تعدد الفروع | ❌ |
| حساب التكلفة وهوامش الربح | ❌ (بيانات التكلفة كلها صفر) |
| دفتر أستاذ محاسبي | ❌ |
| سجل تدقيق غير قابل للتعديل | ❌ مذكور كخطة فقط |
| صلاحيات حقيقية | ❌ |
| العمل دون إنترنت | ❌ مذكور كفكرة |
| شاشة المطبخ KDS | ❌ مذكورة كفكرة |
| التزام ضريبي / فوترة إلكترونية معتمدة | ❌ |
| تكامل بوابات الدفع | ❌ |
| API عام و Webhooks | ❌ |
| اختبارات و CI/CD | ❌ |

**ملاحظة مهمة للسوق العربي:** الفوترة الإلكترونية المعتمدة ليست ميزة تنافسية بل **شرط قانوني** في عدة دول (هيئة الزكاة والضريبة والجمارك في السعودية، منظومة الفاتورة الإلكترونية في مصر). إن كان هدفك البيع في الخليج أو مصر، اجعلها في المرحلة 3 لا في النهاية.

---

## 7️⃣ الخلاصة بجملة واحدة

> **الواجهة والأفكار في مستوى جيد. ما ينقص هو أن النظام لا يحفظ ولا يحاسب.**
> ابدأ بـ `submitOrder()` وطبقة البيانات، لا بمزيد من الشاشات.
> كل شاشة جديدة تُبنى فوق أساس لا يحفظ هي عمل سيُعاد.

---

*المراجعة مبنية على فحص مباشر: بصمات SHA256، تحميل `data.js` في Node لقراءة المخطط الحقيقي، عدّ `innerHTML`/`escapeHtml`، وفحص السكربتات المحمّلة في كل صفحة.*
