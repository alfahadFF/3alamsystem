# 📐 خطة الربط والتكلفة والنسختين — alfaprosys
### تحديث على المراجعة بعد توضيحات صاحب المشروع

*2026-08-31 · مبني على قياس فعلي للكود*

---

## 0️⃣ تصنيف صحيح للاكتشافات

### ✅ قرارات تطويرية مقصودة (ليست عيوبًا)
| البند | الحالة |
|---|---|
| عدم ربط قاعدة البيانات | مقصود — مرحلة تطوير الواجهة |
| عدم حفظ الفواتير (`submitOrder`) | مقصود — الحفظ يأتي مع الربط |
| غياب المستخدمين والأدوار | مقصود — لا مستخدمين في مرحلة التطوير |
| غياب الأوفلاين والمزامنة | لم يُعمل عليه بعد |

> **تصحيح:** المراجعة الأولى قدّمت هذه البنود الأربعة بوصفها "اكتشافات حرجة". هذا التصنيف كان خاطئًا؛ فهي مرحلة مقصودة وليست عيوبًا مجهولة.

### 🔴 يبقى قائمًا ويتطلب قرارًا
| البند | السبب |
|---|---|
| **علاقة الوصفة معكوسة** | خطأ في المخطط، سيُرحَّل إلى قاعدة البيانات إن لم يُصلح قبل الربط |
| **الملاحظات نص حر** | `"note": "ثوم إضافة، بدون حار"` — لا يمكن تسعيره |
| **غياب `tenant_id`** | إضافة التعددية لاحقًا على نظام فيه عميل حقيقي مكلفة جدًا |
| **XSS في 14 شاشة** | يصبح استغلالًا فعليًا لحظة الربط، وليس قبله |

---

## 1️⃣ الربط عبر `data.js` — لماذا لا يعمل كما هو، وكيف يعمل

### القياس
```
إشارات مباشرة لـ DATA ......... 110 إشارة في 15 ملفًا
مواضع تكتب مباشرة في DATA ..... 41 موضعًا
استخدام async/await .......... 0 في المشروع كله
```

القراءة فورية عند التحميل — `cash_reports.js:7-10`:
```js
let session    = DATA.cashierSession   || {};
let invoices   = DATA.invoices         || [];
let expenses   = DATA.expenditures     || [];
let shiftsHist = DATA.shifts_history   || [];
```

### العائقان
1. **تزامن مقابل لاتزامن.** `data.js` يعطي كائنًا متزامنًا. لو جلب من الشبكة، تكون `DATA.invoices` فارغة لحظة تنفيذ السطور أعلاه.
2. **الكتابة لا تمر بـ `data.js`.** مثال `menu_admin.js:593` → `DATA.items.push(newItem)`. تعديل مباشر في الذاكرة لا يعترضه أي ملف آخر.

### الحل: Cache-first / hydrate-then-sync

يبقى `data.js` نقطة الربط الوحيدة، لكنه يقرأ من كاش محلي متزامن ثم يزامن بالخلفية:

```
assets/js/
├── data.js            ← كما هو: يبني window.DEMO_DATA من الكاش (متزامن)
├── sync/
│   ├── cache.js       ← قراءة/كتابة الكاش المحلي
│   ├── queue.js       ← طابور العمليات المعلّقة (أساس الأوفلاين)
│   └── remote.js      ← Supabase: سحب + دفع
└── config.js          ← dev / trial / prod
```

**الدورة:**
```
التحميل → الكاش المحلي (فوري) → الشاشات تعمل بلا تعديل
             ↓ في الخلفية
        Supabase → تحديث الكاش → إعادة رسم

الكتابة → DATA (كما اليوم) + اعتراض عبر Proxy → طابور → إرسال عند توفر الشبكة
```

**النقطة المهمة:** الـ `Proxy` يحل مشكلة الـ41 موضع كتابة **دون تعديل أي شاشة**:

```js
window.DEMO_DATA = new Proxy(cachedData, {
  set(target, key, value) {
    target[key] = value;
    SyncQueue.push({ table: key, op: 'replace', value });  // يُلتقط تلقائيًا
    return true;
  }
});
```

> **ملاحظة مُختبَرة (نُفّذت في Node، لا تقديرًا):** الـ `Proxy` يلتقط الإسناد (`DATA.items = [...]`) لكنه **لا يلتقط** `DATA.items.push(...)`.
>
> نتيجة الاختبار:
> ```
> بعد الإسناد → الطابور: [{"table":"items","op":"replace"}]  ✅
> بعد push    → الطابور: []                                  ❌ لم يُلتقط
> ```

### الحصر الفعلي: 3 مواضع فقط تحتاج تحويلًا

فحصت كل الـ16 موضعًا، فتبيّن أن **11 منها `.filter()` للقراءة فقط** (لا تعدّل شيئًا):

| الملف:السطر | العملية | يلتقطها Proxy؟ |
|---|---|---|
| `menu_admin.js:468` | `DATA.items = DATA.items.filter(...)` | ✅ إسناد |
| `menu_admin.js:569` | `DATA.categories.push(newCat)` | ❌ **يحتاج تحويلًا** |
| `menu_admin.js:593` | `DATA.items.push(newItem)` | ❌ **يحتاج تحويلًا** |
| `menu_admin.js:680` | `DATA.items = DATA.items.filter(...)` | ✅ إسناد |
| `menu_admin.js:685` | `DATA.categories = DATA.categories.filter(...)` | ✅ إسناد |
| `menu_admin.js:729` | `DATA.categories.push(newCat)` | ❌ **يحتاج تحويلًا** |
| الـ11 الباقية | `.filter()` للقراءة في pos/invoices/edit_invoice/open_invoices | — لا تعدّل |

**أي أن كلفة الترحيل كلها: تحويل 3 أسطر في ملف واحد.**

```js
// قبل
DATA.items.push(newItem);
// بعد
DATA.items = [...DATA.items, newItem];
```

**العائد المزدوج:** نفس البنية تمنح الأوفلاين والمزامنة لاحقًا دون إعادة بناء.

---

## 2️⃣ التكلفة: حل مشكلة تقلّب طلبات الزبائن

### المبدأ: لا تحسب تكلفة الطلب — احسب تكلفة الصنف + الإضافات

**الوضع الحالي (لا يُحسب):**
```json
"note": "ثوم زيادة، بدون حار"
```

**المطلوب (يُحسب):**
```js
{
  "item_id": "item_001",
  "qty": 2,
  "modifiers": [
    { "id": "mod_garlic_extra", "cost": 1500 },
    { "id": "mod_no_spicy",     "cost": 0 }
  ]
}
```

**الجداول:**
```sql
modifiers       (id, name, cost, is_default)
item_modifiers  (item_id, modifier_id, price_delta)   -- متاح لهذا الصنف؟
invoice_line_modifiers (invoice_line_id, modifier_id, cost_at_time)
```

> `cost_at_time` ضروري: تكلفة الإضافة وقت البيع، لا وقت القراءة. وإلا تغيّرت أرباح الشهر الماضي عندما تعدّل تكلفة اليوم.

### التكلفة النظرية مقابل الفعلية

لا توجد دقة مطلقة، ولا يلزم:

```
النظرية = Σ (وصفة الصنف × الكمية المباعة)
الفعلية = رصيد أول المدة + مشتريات المدة − جرد آخر المدة
الفرق   = هدر + سرقة + أخطاء حصص  ← أهم رقم للإدارة
```

هذا ما تعتمده الأنظمة العالمية. الفرق بنسبة 2–4% طبيعي؛ فوق 8% يعني مشكلة تشغيلية.

### المتوسط المرجّح لتكلفة المواد

```
التكلفة الجديدة = (الرصيد × التكلفة القديمة + الكمية الواردة × سعرها)
                 ÷ (الرصيد + الكمية الواردة)
```
يُحسب تلقائيًا عند كل أمر شراء — لا إدخال يدوي للتكاليف.

### قاعدة 80/20

| الشريحة | عدد الأصناف | الدقة |
|---|---|---|
| الأعلى إيرادًا | ~20 من 119 | وصفة كاملة مفصّلة |
| المتوسطة | ~40 | وصفة مبسّطة |
| الباقية | ~59 | نسبة تكلفة تقديرية على مستوى التصنيف |

### الحصص القياسية
`portion_size` لكل مادة في الوصفة (مثال: 150غ دجاج للسندويش). **ضبط الحصص هو الرافعة الحقيقية للربح، لا دقة الحساب.**

---

## 3️⃣ النسختان: تجريبية + عميل حالي

### ⚠️ قاعدة: لا مشروع Supabase واحد للنسختين

أي خطأ في سياسات RLS يخلط بيانات العميل الحقيقي بالتجريبية — وهذا حادث لا يُغتفر تجاريًا.

| البيئة | المشروع | المحتوى |
|---|---|---|
| `dev` | مستقل | تطوير وتعديل بحرية |
| `trial` | مستقل | بيانات تجريبية + **زر إعادة ضبط** |
| `prod` | مستقل | بيانات العميل الحقيقي |

### انتهاء مدة التجريب — يُفرض على الخادم

```sql
-- سياسة RLS: لا وصول بعد انتهاء المدة
create policy "trial_not_expired" on invoices
  for all
  using (
    (select expires_at from tenants where id = tenant_id) > now()
  );
```

> **لا تضع تاريخ الانتهاء في JavaScript.** أي مستخدم يعدّله من أدوات المطوّر خلال ثوانٍ.

### `tenant_id` من اليوم الأول

```sql
-- كل جدول يبدأ به
tenant_id uuid not null references tenants(id)

-- وفهرس مركّب في كل استعلام
create index on invoices (tenant_id, created_at desc);
```

إضافته لاحقًا على نظام فيه عميل حقيقي = ترحيل بيانات + خطر فقد. إضافته الآن = عمود واحد.

### ميزة بيع قوية للنسخة التجريبية

زر **"إعادة ضبط البيانات التجريبية"** يعيد المطعم الكامل (119 صنفًا، فواتير، موظفون، عقود) لحالته الأولى. يُنفَّذ بإجراء مخزّن على الخادم:

```sql
create function reset_trial_data(p_tenant uuid) returns void ...
```

---

## 4️⃣ ترتيب التنفيذ المقترح

| # | المهمة | السبب في هذا الترتيب |
|---|---|---|
| 1 | `tenant_id` في المخطط | أرخص ما يمكن الآن، وأغلى ما يمكن لاحقًا |
| 2 | إصلاح اتجاه الوصفات | قبل ترحيل بيانات المخزون لقاعدة البيانات |
| 3 | توحيد `common.js` | 15 تعريفًا لـ`fmtNum` تعني 15 موضع إصلاح لكل تعديل |
| 4 | `cache.js` + `queue.js` | الأساس المشترك للربط والأوفلاين |
| 5 | `remote.js` (Supabase) | بعد ثبات الطبقة تحته |
| 6 | `modifiers` المهيكلة | تفتح حساب التكلفة |
| 7 | محرك التكلفة والفرق | بعد توفر الإضافات والتكلفة |
| 8 | `escapeHtml` في 14 شاشة | قبل استقبال بيانات مستخدمين حقيقيين |
| 9 | RLS + انتهاء التجريب | قبل منح أي نسخة تجريبية لعميل |

---

*مبني على فحص مباشر: تحميل `data.js` في Node، عدّ الإشارات المباشرة لـ`DATA`، حصر مواضع الكتابة، وفحص السكربتات المحمّلة في كل صفحة.*
