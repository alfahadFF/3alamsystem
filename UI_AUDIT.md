# 🎨 تدقيق الواجهة — alfaprosys
### ماذا ينقصها لتصبح راقية وعالمية؟ (نسخة ما قبل الربط = النسخة التجريبية)

*2026-08-31 · مبني على: لقطات شاشة حقيقية (جوال 390px + حاسوب 1440px) وفحص كمي لملفات CSS/HTML/JS*

---

## ✅ ما هو قوي فعلًا (مُقاس، لا انطباعًا)

| الميزة | الدليل |
|---|---|
| اتجاه RTL سليم ولغة عربية | `lang="ar" dir="rtl"` في كل الصفحات |
| نظام رموز (Design Tokens) موجود | 19 متغيرًا في `:root` بألوان دلالية |
| عمل استجابة حقيقي | **79 media query** على 4 نقاط كسر |
| حالات التركيز للمفاتيح | 23 حالة `:focus/:focus-visible` |
| حالات فارغة | 40 موضع empty-state في JS |
| أنماط طباعة | 2 `@media print` |
| تباين النص الأساسي | 17.06 (AAA) · النص الخافت 4.55 (AA) |
| مراعاة الشاشات المقتطعة | `env(safe-area-inset-*)` |
| خطوط عربية جيدة | Changa للعناوين + Almarai للنص |

**الانطباع البصري من اللقطات:** نظيف ومنسجم، بطاقات بزوايا موحّدة (14px) ومسافات مريحة. أساس بصري جيد جدًا يبني عليه.

---

## 🔴 الفجوات إلى "راقي وعالمي" — مرتّبة بالأثر على نموذج عملك

### 1️⃣ الرموز معرّفة لكن غير مستخدمة → إعادة التسمية للعملاء مستحيلة

```
متغيرات :root ........ 19
ألوان #hex ثابتة ....... 426
دوال rgb()/hsl() ....... 186
```

هذا **أخطر فجوة لنموذج عملك تحديداً**: أنت تبيع النظام لعدة عملاء. العميل الحالي يريد ألوانه وشعاره، والنسخة التجريبية تريد هويتك. طالما أن 426 لوناً مكتوبة يدوياً في الـ CSS، فكل عميل جديد = نسخة CSS معدّلة يدوياً.

**الحل:** استبدال الـ426 لوناً بالرموز التسعة عشر (وتوسيعها)، فيصبح تغيير هوية عميل كامل = تعديل ملف `theme.css` واحد. هذا هو معنى "white-label" الذي تبيعه الأنظمة العالمية (Toast, Lightspeed) بفاتورة أعلى.

### 2️⃣ لا أيقونات — فقط Emoji

```
وسوم <svg> في 16 صفحة .... 3  (الشعار فقط)
```

الواجهة كلها تبني أيقوناتها على الإيموجي (🌯 👑 ️). الإيموجي:
- يختلف رسمها بين أندرويد وiOS وويندوز (لا اتساق بصري).
- لا يمكن تلوينها بلون العلامة (تبقى ملوّنة دائماً).
- تعطي انطباع "تجربة" لا "منتج".

**الحل:** نظام أيقونات SVG مضمّن (مثلاً مجموعة موحّدة Stroke 24px). الشعار نفسه SVG — أي أن المسار التقني معروف عندك، فقط يُعمَّم.

### 3️⃣ الـ POS يفتح فارغًا

```js
let activeCategoryId = null;   // pos.js:3
```

لقطة الشاشة تؤكد: مساحة الأصناف بيضاء بالكامل مع "اختر الأصناف من القائمة". كاشير في ذروة العمل لا يجب أن يبدأ بفراغ.

**الحل:** عرض الأصناف الأكثر مبيعًا أو الكل افتراضيًا + شريط بحث دائم. (الحقل `is_pinned_popular` موجود في بياناتك — جاهز للاستخدام.)

### 4️⃣ تباين ألوان الحالة يفشل AA للنص العادي

محسوب (النص العادي يحتاج 4.5):

| الاستخدام | النسبة | الحكم |
|---|---|---|
| نص أزرق على أبيض | 4.10 | ❌ AA كبير فقط |
| نص ذهبي على أبيض | 3.19 | ❌ |
| نص أخضر على أبيض | 3.30 | ❌ |
| نص أحمر على أبيض | 4.83 | ✅ AA |

**الحل:** استخدام درجات أغمق (`#0369A1` للأزرق، `#B45309` للذهبي، `#15803D` للأخضر) عندما يكون اللون "نصًا"، وإبقاء الفاتح للأزرار ذات النص الأبيض.

### 5️⃣ لا وضع ليلي ولا احترام تفضيل الحركة

```
prefers-color-scheme .... 0
prefers-reduced-motion .. 0
```

الوضع الليلي معيار متوقع اليوم (ومفيد لصالات المطاعم المعتمة)، و`reduced-motion` شرط WCAG 2.1. كلاهما يأتي **مجانًا** إذا أنجزت النقطة 1 (الرموز).

### 6️⃣ لا PWA — مع أنك تخطط للأوفلاين

```
manifest.json ...... 0
service worker ..... 0
favicon / apple-touch-icon .. 0
```

نسخة تجريبية يُفترض أن "يجرّبها العميل بالكامل" يجب أن تُثبَّت على جواله مثل تطبيق. الـ PWA (أيقونة على الشاشة، شاشة انطلاق، عمل دون إنترنت) هو ما يجعل التجربة "تطبيقاً" لا "موقعاً" — وهذا فرق بيع حاسم.

### 7️⃣ لا حالات تحميل (Skeletons)

```
skeleton/spinner .... 0
```

عند الربط بقاعدة البيانات ستظهر فترات انتظار. الأنظمة الراقية تعرض هياكل رمادية نابضة لا فراغاً. ابنِها الآن في النسخة التجريبية لتبدو النسخة الحية سريعة من اليوم الأول.

### 8️⃣ لا إدخال سريع: باركود ولا كيبورد

```
barcode / keydown ... 0
```

كاشير محترف لا ينقر على كل صنف. الأنظمة العالمية تدعم: ماسح باركود، اختصارات أرقام، وأسهم تنقّل. أضف مستمع `keydown` واحد في `pos.js` — تكلفة صغيرة وأثر كبير على "الإحساس الاحترافي".

### 9️⃣ الطباعة ليست حرارية

`@media print` موجود لكنه عام. إيصالات المطاعم تُطبع على 58/80مم. تحتاج أنماط طباعة مخصّصة بعرض ثابت وخط مونوسبيس للأرقام وفصل واضح — وإلا خرج الإيصال مشوّهًا على طابعة المطعم الفعلية.

---

## 📋 خلاصة الأولويات (للنسخة التجريبية قبل الربط)

| # | البند | الجهد | الأثر على "راقي/عالمي" |
|---|---|---|---|
| 1 | توحيد الرموز (theming) | متوسط | ⭐⭐⭐⭐⭐ (بيع لعدة عملاء) |
| 2 | نظام أيقونات SVG | متوسط | ⭐⭐⭐⭐⭐ (انطباع المنتج) |
| 3 | POS يعرض افتراضيًا | صغير | ⭐⭐⭐⭐ |
| 4 | تباين AA للألوان | صغير | ⭐⭐⭐⭐ (وصول + احتراف) |
| 5 | وضع ليلي + reduced-motion | صغير بعد 1 | ⭐⭐⭐ |
| 6 | PWA + أيقونة | متوسط | ⭐⭐⭐⭐ (تجربة "تطبيق") |
| 7 | Skeletons | صغير | ⭐⭐⭐ |
| 8 | كيبورد/باركود | صغير | ⭐⭐⭐⭐ |
| 9 | طباعة حرارية | صغير | ⭐⭐⭐⭐ (واقع المطعم) |

---

*الأدلة: لقطات Puppeteer (390px/1440px)، عدّ `@media` و`svg` و`skeleton`، حساب نسب التباين في Node، وفحص `pos.js:3` و`@media print`.*
